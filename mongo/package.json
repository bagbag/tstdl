{
  "name": "@tstdl/mongo",
  "version": "0.12.0",
  "author": "Patrick Hein",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "pub": "npm run build && cp package.json package-lock.json dist/ && npm publish dist"
  },
  "dependencies": {
    "@tstdl/base": "^0.46.0",
    "@tstdl/database": "^0.4.0",
    "@tstdl/server": "^0.20.2",
    "mongodb": "^3.3.2"
  },
  "devDependencies": {
    "@types/mongodb": "^3.3.2",
    "typescript": "~3.6.3"
  }
}
