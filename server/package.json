{
  "name": "@common-ts/server",
  "version": "2.6.0",
  "author": "Patrick Hein",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "pub": "npm run build && cp package.json dist/ && cp package-lock.json dist/ && npm publish dist"
  },
  "dependencies": {
    "@common-ts/base": "^1.12.0",
    "koa": "^2.7.0",
    "koa-router": "^7.4.0"
  },
  "devDependencies": {
    "@types/koa": "^2.0.0",
    "@types/koa-router": "^7.0.0",
    "typescript": "~3.4.0"
  }
}
