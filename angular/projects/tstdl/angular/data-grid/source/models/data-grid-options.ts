import type { DynamicText } from '@tstdl/base/text';

export type DataGridDefaultOptions = {
  booleanTrueText?: DynamicText,
  booleanFalseText?: DynamicText,
  noValueText?: DynamicText,
  itemColumns?: number
};
