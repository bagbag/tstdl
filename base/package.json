{
  "name": "@tstdl/base",
  "version": "0.55.2",
  "author": "Patrick Hein",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "pub": "npm run build && cp package.json package-lock.json tslint.json tsconfig.json dist/ && npm publish dist"
  },
  "dependencies": {
    "rxjs": "^6.5.4"
  },
  "devDependencies": {
    "@types/node": "^12.12.24",
    "typescript": "~3.6.4"
  }
}
