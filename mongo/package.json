{
  "name": "@tstdl/mongo",
  "version": "0.9.1",
  "author": "Patrick Hein",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "pub": "npm run build && cp package.json package-lock.json dist/ && npm publish dist"
  },
  "dependencies": {
    "@tstdl/base": "^0.37.3",
    "@tstdl/database": "^0.3.0",
    "@tstdl/server": "^0.17.0",
    "mongodb": "^3.3.2"
  },
  "devDependencies": {
    "@types/mongodb": "^3.3.1",
    "typescript": "~3.6.2"
  }
}
