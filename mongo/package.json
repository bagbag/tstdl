{
  "name": "@tstdl/mongo",
  "version": "0.19.0",
  "author": "Patrick Hein",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "pub": "npm run build && cp package.json package-lock.json dist/ && npm publish dist"
  },
  "dependencies": {
    "@tstdl/base": "^0.50.0",
    "@tstdl/database": "^0.8.1",
    "@tstdl/server": "^0.26.0",
    "mongodb": "^3.3.5"
  },
  "devDependencies": {
    "@types/mongodb": "^3.3.11",
    "typescript": "~3.7.2"
  }
}
