{
  "name": "@tstdl/mongo",
  "version": "0.13.0",
  "author": "Patrick Hein",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "pub": "npm run build && cp package.json package-lock.json dist/ && npm publish dist"
  },
  "dependencies": {
    "@tstdl/base": "^0.47.0",
    "@tstdl/database": "^0.5.0",
    "@tstdl/server": "^0.21.0",
    "mongodb": "^3.3.2"
  },
  "devDependencies": {
    "@types/mongodb": "^3.3.3",
    "typescript": "~3.6.3"
  }
}
