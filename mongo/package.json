{
  "name": "@tstdl/mongo",
  "version": "0.21.2",
  "author": "Patrick Hein",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "pub": "npm run build && cp package.json package-lock.json dist/ && npm publish dist"
  },
  "dependencies": {
    "@tstdl/base": "^0.55.2",
    "@tstdl/database": "^0.8.7",
    "@tstdl/server": "^0.27.0",
    "mongodb": "^3.5.2"
  },
  "devDependencies": {
    "@types/mongodb": "^3.3.15",
    "typescript": "~3.7.5"
  }
}
