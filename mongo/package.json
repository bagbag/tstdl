{
  "name": "@tstdl/mongo",
  "version": "0.8.3",
  "author": "Patrick Hein",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "pub": "npm run build && cp package.json package-lock.json dist/ && npm publish dist"
  },
  "dependencies": {
    "@tstdl/base": "^0.35.2",
    "@tstdl/database": "^0.2.0",
    "@tstdl/server": "^0.9.1",
    "mongodb": "^3.2.7"
  },
  "devDependencies": {
    "@types/mongodb": "^3.1.28",
    "typescript": "~3.5.2"
  }
}
