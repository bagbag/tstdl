{
  "name": "@tstdl/mongo",
  "version": "0.20.1",
  "author": "Patrick Hein",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "pub": "npm run build && cp package.json package-lock.json dist/ && npm publish dist"
  },
  "dependencies": {
    "@tstdl/base": "^0.53.2",
    "@tstdl/database": "^0.8.2",
    "@tstdl/server": "^0.26.1",
    "mongodb": "^3.4.1"
  },
  "devDependencies": {
    "@types/mongodb": "^3.3.14",
    "typescript": "~3.7.4"
  }
}
