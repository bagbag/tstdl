{
  "name": "@tstdl/mongo",
  "version": "0.22.0",
  "author": "Patrick Hein",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "pub": "npm run build && cp package.json package-lock.json dist/ && npm publish dist"
  },
  "dependencies": {
    "@tstdl/base": "^0.56.0",
    "@tstdl/database": "^0.9.0",
    "@tstdl/server": "^0.28.0",
    "mongodb": "^3.5.3"
  },
  "devDependencies": {
    "@types/mongodb": "^3.3.16",
    "typescript": "~3.7.5"
  }
}
