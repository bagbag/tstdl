{
  "name": "@tstdl/database",
  "version": "0.9.0",
  "author": "Patrick Hein",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "pub": "npm run build && cp package.json package-lock.json dist/ && npm publish dist"
  },
  "dependencies": {
    "@tstdl/base": "^0.56.0"
  },
  "devDependencies": {
    "typescript": "~3.7.5"
  }
}
