{
  "name": "@common-ts/mongo",
  "version": "1.4.1",
  "description": "",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "pub": "npm run build && cp package.json dist/ && cp package-lock.json dist/ && npm publish dist --access=public"
  },
  "author": "Patrick Hein",
  "dependencies": {
    "@common-ts/base": "^1.9.0",
    "mongodb": "^3.2.3"
  },
  "devDependencies": {
    "@types/mongodb": "^3.1.22",
    "typescript": "^3.4.3"
  }
}
