{
  "name": "@common-ts/redis",
  "version": "1.2.1",
  "author": "Patrick Hein",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && tsc && cp src/lua/*.lua dist/lua",
    "pub": "npm run build && cp package.json dist/ && cp package-lock.json dist/ && npm publish dist"
  },
  "dependencies": {
    "@common-ts/base": "^1.35.1",
    "@common-ts/server": "^2.9.0",
    "ioredis": "^4.10.0",
    "rxjs": "^6.5.2"
  },
  "devDependencies": {
    "@types/ioredis": "^4.0.12",
    "typescript": "~3.5.2"
  }
}
