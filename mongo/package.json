{
  "name": "@tstdl/mongo",
  "version": "0.23.1",
  "author": "Patrick Hein",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "pub": "npm run build && cp package.json package-lock.json dist/ && npm publish dist"
  },
  "dependencies": {
    "@tstdl/base": "^0.56.1",
    "@tstdl/database": "^0.9.1",
    "@tstdl/server": "^0.28.2",
    "mongodb": "^3.5.3"
  },
  "devDependencies": {
    "@types/mongodb": "^3.3.16",
    "typescript": "~3.7.5"
  }
}
