{
  "name": "@tstdl/server",
  "version": "0.27.4",
  "author": "Patrick Hein",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "pub": "npm run build && cp package.json package-lock.json tsconfig.json dist/ && npm publish dist"
  },
  "dependencies": {
    "@tstdl/base": "^0.55.7",
    "koa": "^2.11.0",
    "@koa/router": "^8.0.7"
  },
  "devDependencies": {
    "@types/koa": "^2.11.0",
    "@types/koa__router": "^8.0.2",
    "@types/node": "^12.12.26",
    "typescript": "~3.7.5"
  }
}
