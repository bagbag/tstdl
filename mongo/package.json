{
  "name": "@tstdl/mongo",
  "version": "0.15.0",
  "author": "Patrick Hein",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "pub": "npm run build && cp package.json package-lock.json dist/ && npm publish dist"
  },
  "dependencies": {
    "@tstdl/base": "^0.48.3",
    "@tstdl/database": "^0.5.1",
    "@tstdl/server": "^0.21.1",
    "mongodb": "^3.3.2"
  },
  "devDependencies": {
    "@types/mongodb": "^3.3.4",
    "typescript": "~3.6.3"
  }
}
