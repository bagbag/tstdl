{
  "name": "@tstdl/mongo",
  "version": "0.20.2",
  "author": "Patrick Hein",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "pub": "npm run build && cp package.json package-lock.json dist/ && npm publish dist"
  },
  "dependencies": {
    "@tstdl/base": "^0.53.2",
    "@tstdl/database": "^0.8.3",
    "@tstdl/server": "^0.26.4",
    "mongodb": "^3.4.1"
  },
  "devDependencies": {
    "@types/mongodb": "^3.3.14",
    "typescript": "~3.7.4"
  }
}
