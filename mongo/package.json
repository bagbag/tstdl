{
  "name": "@tstdl/mongo",
  "version": "0.16.1",
  "author": "Patrick Hein",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "pub": "npm run build && cp package.json package-lock.json dist/ && npm publish dist"
  },
  "dependencies": {
    "@tstdl/base": "^0.49.0",
    "@tstdl/database": "^0.7.0",
    "@tstdl/server": "^0.25.1",
    "mongodb": "^3.3.3"
  },
  "devDependencies": {
    "@types/mongodb": "^3.3.7",
    "typescript": "~3.6.4"
  }
}
