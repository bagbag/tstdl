{
  "name": "@common-ts/database",
  "version": "1.2.0",
  "author": "Patrick Hein",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "pub": "npm run build && cp package.json dist/ && cp package-lock.json dist/ && npm publish dist"
  },
  "dependencies": {
    "@common-ts/base": "^1.12.0"
  },
  "devDependencies": {
    "typescript": "~3.4.0"
  }
}
