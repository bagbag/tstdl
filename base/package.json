{
  "name": "@common-ts/base",
  "version": "1.14.0",
  "author": "Patrick Hein",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "pub": "npm run build && cp package.json dist/ && cp package-lock.json dist/ && cp tslint.json dist/ && npm publish dist"
  },
  "dependencies": {
    "rxjs": "^6.4.0"
  },
  "devDependencies": {
    "@types/node": "^11.13.5",
    "typescript": "~3.4.3"
  }
}
