{
  "name": "@common-ts/client",
  "version": "1.0.1",
  "description": "",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "pub": "npm run build && cp package.json dist/ && cp package-lock.json dist/ && npm publish dist --access=public"
  },
  "author": "Patrick Hein",
  "dependencies": {},
  "devDependencies": {
    "typescript": "~3.4.0"
  }
}
