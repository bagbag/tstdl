{
  "name": "@tstdl/mongo",
  "version": "0.8.3",
  "author": "Patrick Hein",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "pub": "npm run build && cp package.json dist/ && cp package-lock.json dist/ && npm publish dist"
  },
  "dependencies": {
    "@tstdl/base": "^1.17.0",
    "@tstdl/database": "^1.2.0",
    "@tstdl/server": "^2.8.0",
    "mongodb": "^3.2.3"
  },
  "devDependencies": {
    "@types/mongodb": "^3.1.26",
    "typescript": "~3.4.5"
  }
}
